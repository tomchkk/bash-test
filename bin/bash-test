#!/usr/bin/env bash

##
# Starts the main process
##
bash_test_bin::main() {
    local script=$(readlink ${BASH_SOURCE[0]} || echo ${BASH_SOURCE[0]})
    local app_dir="$(cd $(dirname "$script"); pwd)/.."

    source "$app_dir/bash-test" "$app_dir" $@
}

bash_test_bin::main $@
